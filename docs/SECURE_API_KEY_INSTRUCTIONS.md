# Инструкция по безопасной передаче API ключей для тестирования

## ⚠️ ВАЖНО: Безопасность

API ключи НИКОГДА не должны:
- ❌ Коммититься в git
- ❌ Отображаться в истории команд
- ❌ Сохраняться в открытом виде
- ❌ Передаваться через незащищенные каналы

## ✅ Безопасные способы передачи

### Способ 1: Через временный файл (рекомендуется)

1. **Создайте временный файл на сервере:**
   ```bash
   ssh litellm@10.10.0.227 "cat > /tmp/api_keys_temp.txt << 'EOF'
   ANTHROPIC_API_KEY=your-anthropic-key-here
   AZURE_API_KEY=your-azure-key-here
   AZURE_API_BASE=your-azure-endpoint-here
   AZURE_API_VERSION=2024-02-15-preview
   EOF"
   ```

2. **После тестирования удалите файл:**
   ```bash
   ssh litellm@10.10.0.227 "rm -f /tmp/api_keys_temp.txt"
   ```

### Способ 2: Через stdin (самый безопасный)

1. **Создайте файл локально с ключами:**
   ```bash
   # На вашем локальном компьютере
   cat > /tmp/keys_local.txt << 'EOF'
   ANTHROPIC_API_KEY=your-key
   AZURE_API_KEY=your-key
   EOF
   ```

2. **Передайте через stdin без сохранения на сервере:**
   ```bash
   cat /tmp/keys_local.txt | ssh litellm@10.10.0.227 "cat > /tmp/api_keys_temp.txt"
   ```

3. **Удалите локальный файл:**
   ```bash
   rm -f /tmp/keys_local.txt
   ```

### Способ 3: Через переменные окружения (для одноразового использования)

```bash
# Передайте через переменные окружения в одной команде
ssh litellm@10.10.0.227 "ANTHROPIC_API_KEY='your-key' AZURE_API_KEY='your-key' ./test_script.sh"
```

## 🔒 Дополнительные меры безопасности

1. **Ограничьте доступ к файлу:**
   ```bash
   ssh litellm@10.10.0.227 "chmod 600 /tmp/api_keys_temp.txt"
   ```

2. **Используйте временные ключи для тестирования** (если возможно)

3. **Удалите ключи сразу после тестирования**

4. **Проверьте, что ключи не попали в логи:**
   ```bash
   ssh litellm@10.10.0.227 "grep -r 'ANTHROPIC_API_KEY\|AZURE_API_KEY' /tmp /var/log 2>/dev/null || echo 'Keys not found in logs'"
   ```

## 📝 План использования

1. Вы передаете ключи через один из безопасных способов
2. Я использую их для добавления провайдеров через UI
3. Проверю работу моделей через API
4. **Сразу после тестирования удалю все временные файлы с ключами**

## ✅ Чек-лист после тестирования

- [ ] Удален временный файл с ключами
- [ ] Проверено, что ключи не попали в git
- [ ] Проверено, что ключи не попали в логи
- [ ] Проверено, что ключи не сохранены в .env (если не нужно)

