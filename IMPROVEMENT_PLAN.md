# Улучшение кода AI Gateway: рефакторинг, тестирование, документация

## 📊 Metadata

**Artifact Version:** 2.0  
**Last Adaptation Date:** 2025-01-27  
**Purpose:** Долгосрочная карта улучшения кода проекта AI Gateway  
**How to use for AI agent:** See section "🤖 Instructions for AI agent" at the end of this document

**Status:** 🔴 BLOCKED  
**Current Phase:** Фаза 4: Тестирование  
**Current Step:** Этап 6.3: E2E тесты  
**Last Update:** 2025-01-27

---

## 🎯 Current Focus

> **Current Step:** [Фаза 4, Шаг 4.3: E2E тесты](#шаг-43-e2e-тесты)  
> **Status:** 🔴 BLOCKED  
> **Requires Action:** Yes

---

## 🎯 Описание

План систематического улучшения кода проекта AI Gateway для обеспечения готовности репозитория как для человека, так и для AI агента. Включает настройку инструментов качества кода, рефакторинг legacy кода, добавление тестов, улучшение документации и обработки ошибок.

**Цель:** Обеспечить готовность репозитория для работы как с человеком, так и с AI агентом через систематическое улучшение качества кода, архитектуры и документации.

**Бизнес-ценность:** Повышение качества кода, снижение технического долга, улучшение поддерживаемости и готовности к дальнейшему развитию проекта.

---

## 🚦 Quick Navigation for AI agent

**Quick Reference:** This artifact is part of a 4-artifact system (PLAN, CHANGELOG, QUESTIONS, SESSION_CONTEXT). For full instructions on working with this artifact, see "🤖 Instructions for AI agent" section at the end of this document.

- **Start here:** Фаза 4, Этап 6.3 (E2E тесты) - **ЗАБЛОКИРОВАН**
- **Blockers:** See @IMPROVEMENT_QUESTIONS.md (Q2.1, Q2.2)
- **Recent changes:** See @IMPROVEMENT_CHANGELOG.md (Фаза 4, Шаг 4.1-4.3)

---

> **📋 Связанные файлы:**
>
> - `IMPROVEMENT_QUESTIONS.md` - база знаний (сомнения и решения)
> - `IMPROVEMENT_CHANGELOG.md` - Git-подобная история (что сделано)
> - `IMPROVEMENT_SESSION_CONTEXT.md` - оперативная память (текущее состояние)

---

## 📁 Контекст проекта

### Архитектура

- ✅ Слоистая архитектура (core, infrastructure, application)
- ✅ Разделение ответственности между слоями
- ✅ Использование паттерна Repository для файловых операций
- ✅ Централизованная конфигурация через `AppConfig`

### Проблемы и области для улучшения

1. **Legacy код** ✅ Частично решено (Этап 3)
   - ✅ `utils.py` преобразован в wrapper, используется только для `check_root()` и `get_user()` (TODO: переместить в `infrastructure/platform_utils.py`)
   - ✅ `config.py` оставлен как re-export из `core.config` для обратной совместимости
   - ⚠️ `env_generator.py` частично использует новый SecurityService, но содержит fallback

2. **Качество кода** ✅ Частично решено (Этапы 1, 2, 4)
   - ✅ Настроены линтеры (ruff, black, mypy) - Этап 1
   - ✅ Добавлены type hints во всех модулях - Этап 2
   - ✅ Устранено дублирование кода - Этап 4
   - ⏳ Тесты: unit и integration выполнены, E2E тесты в процессе - Этап 6

3. **Документация кода** ✅ Решено (Этап 7)
   - ✅ Все публичные функции имеют docstrings в формате Google style
   - ✅ Docstrings краткие и ссылаются на документацию в `docs/`

4. **Обработка ошибок** ✅ Решено (Этап 5)
   - ✅ Все кастомные исключения наследуются от `AIGatewayError`
   - ✅ Добавлен контекст к исключениям через `logger.error()` с `exc_info=True`
   - ✅ Все исключения используют цепочку (`from e`) для сохранения контекста

5. **Архитектурные проблемы** (проверено 2025-01-27):
   - ✅ **Нет циклических зависимостей**: Проверено - `core/` не импортирует из `infrastructure/` или `application/`
   - ✅ **Слои соблюдены**: `infrastructure/` зависит от `core/`, `application/` зависит от `core/` и `infrastructure/`
   - ⚠️ **TODO в коде**: `setup_service.py` использует `check_root` и `get_user` из `utils.py` (TODO: переместить в `infrastructure/platform_utils.py`)

### Приоритеты

**Высокий приоритет:**

1. Настройка инструментов качества кода (Этап 1) ✅ Выполнено
2. Улучшение type hints (Этап 2) ✅ Выполнено
3. Миграция legacy кода (Этап 3) ✅ Выполнено

**Средний приоритет:**
4. Рефакторинг и уменьшение дублирования (Этап 4) ✅ Выполнено
5. Улучшение обработки ошибок (Этап 5) ✅ Выполнено
6. Добавление unit тестов (Этап 6.1) ✅ Выполнено
7. Добавление integration тестов (Этап 6.2) ✅ Выполнено
8. Улучшение документации кода (Этап 7) ✅ Выполнено
9. Оптимизация импортов (Этап 8) ✅ Выполнено
10. Улучшение читаемости и навигации артефактов (Этап 10) ✅ Выполнено
11. Анализ и создание BaseService (Этап 9) ⏳ Ожидает начала

**Низкий приоритет (последний шаг):**
12. Добавление E2E тестов (Этап 6.3) - **обязательно для готовности репозитория для AI агентов, но выполняется в последнюю очередь** ⏳ В процессе

### Метрики успеха

- [x] Все модули имеют полные type hints (активный код)
- [x] Mypy проходит без ошибок для активного кода (33 файла без ошибок)
- [x] Ruff проходит без ошибок (All checks passed!)
- [x] Ruff форматирует код единообразно (35 файлов отформатированы)
- [x] Pre-commit hooks настроены (конфигурация создана)
- [x] Покрытие тестами: стремиться к 100% для core и infrastructure
  - `core/constants.py`: 100%
  - `core/exceptions.py`: 100%
  - `core/config.py`: 100%
  - `infrastructure/file_repository.py`: 92%
  - `infrastructure/security.py`: 100%
  - `infrastructure/logger.py`: 100%
  - `infrastructure/output.py`: 85%
  - `infrastructure/docker_client.py`: 90%
  - `application/services.py`: 88%
- [x] Все legacy модули мигрированы (utils.py преобразован в wrapper)
- [x] Все кастомные исключения наследуются от `AIGatewayError`
- [x] Все исключения используют цепочку (`from e`) для сохранения контекста
- [x] Нет циклических зависимостей
- [x] Все публичные функции имеют docstrings в формате Google style

### Современные стандарты и инструменты (2024-2025)

**Выбранные инструменты:**

1. **Ruff** - линтер и форматтер (заменяет flake8, isort, pyupgrade)
2. **Black** - форматтер кода (де-факто стандарт)
3. **mypy** - статическая проверка типов
4. **pre-commit** - hooks для автоматических проверок
5. **pytest** - фреймворк для тестирования

**Покрытие тестами:**

- **70-80%** - современный стандарт для production проектов
- Фокус на core и infrastructure слоях
- Unit тесты с моками для Docker API (не требуют реального Docker)

---

## Фазы имплементации

### Фаза 1: Инструменты и качество кода

**Контекст:** Настройка современных инструментов качества кода и улучшение типизации для обеспечения единообразия стиля, выявления ошибок на раннем этапе и улучшения читаемости кода.  
**Цель:** Обеспечить единообразие стиля кода, автоматическую проверку качества и типизацию для снижения количества ошибок.  
**Статус:** 🟢 COMPLETED

#### Шаг 1.1: Настройка инструментов качества кода

**Статус:** 🟢 Done

**Что нужно сделать:**

- Добавить `pyproject.toml` с настройками для ruff, black, mypy, pytest
- Настроить pre-commit hooks через `.pre-commit-config.yaml`
- Создать `requirements-dev.txt` для dev зависимостей
- Запустить `ruff check . --fix` для исправления существующих проблем
- Исправить ошибки mypy в активном коде

**Почему именно так:**

- Современные инструменты качества кода (2024-2025) обеспечивают единообразие стиля, выявляют ошибки на раннем этапе, экономят время на ревью
- Ruff вместо flake8: Написан на Rust, работает в 10-100 раз быстрее, объединяет функциональность flake8, isort, pyupgrade
- Black: Де-факто стандарт для Python проектов, обеспечивает единообразие стиля без споров
- mypy: Статическая проверка типов снижает количество runtime ошибок, особенно важно для кода, пишущегося AI агентом
- pre-commit hooks: Автоматический запуск проверок перед коммитом экономит время на CI/CD, стандартная практика

**Где делать изменения:**

- Файлы: `pyproject.toml`, `.pre-commit-config.yaml`, `requirements-dev.txt`
- Команды: `ruff check . --fix`, `mypy src/`

**Критерии завершения:**

- [x] `pyproject.toml` создан с настройками для всех инструментов
- [x] `.pre-commit-config.yaml` создан с hooks для всех инструментов
- [x] `requirements-dev.txt` создан с dev зависимостями
- [x] Все проверки ruff проходят успешно
- [x] Ошибки mypy исправлены в активном коде
- [ ] [TODO: Добавить обоснование] Установить pre-commit hooks: `pre-commit install`

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 1

---

#### Шаг 1.2: Улучшение type hints

**Статус:** 🟢 Done

**Что нужно сделать:**

- Проверить все модули на наличие type hints
- Добавить type hints где отсутствуют
- Настроить mypy и исправить ошибки типизации
- Добавить `# type: ignore` только там, где действительно необходимо
- Legacy модули настроить на игнорирование через mypy config

**Почему именно так:**

- Type hints улучшают читаемость кода, помогают IDE с автодополнением, mypy находит ошибки до выполнения
- Важно для AI агентов: Type hints помогают модели лучше понимать код, снижают вероятность ошибок при генерации кода
- Python 3.8+ совместимость: Используем `Union[X, Y]` вместо `X | Y` (требует Python 3.10+), `Optional[X]` вместо `X | None`

**Где делать изменения:**

- Файлы: Все модули в `src/core/`, `src/infrastructure/`, `src/application/`
- Команды: `mypy src/`, `mypy src/core/ src/infrastructure/`

**Критерии завершения:**

- [x] Все модули проверены на наличие type hints
- [x] Type hints добавлены где отсутствуют
- [x] Ошибки типизации исправлены (33 файла без ошибок)
- [x] Legacy модули настроены на игнорирование через mypy config

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 2

---

### Фаза 2: Рефакторинг и миграция

**Контекст:** Миграция legacy кода в новую слоистую архитектуру и устранение дублирования для улучшения поддерживаемости и тестируемости.  
**Цель:** Обеспечить соответствие кода слоистой архитектуре (core, infrastructure, application) и устранить дублирование кода.  
**Статус:** 🟢 COMPLETED

#### Шаг 2.1: Миграция legacy кода

**Статус:** 🟢 Done

**Что нужно сделать:**

- Создать модуль `infrastructure/output.py` с функциями форматированного вывода
- Мигрировать все модули application слоя на использование `infrastructure/output.py`
- Преобразовать `utils.py` в wrapper для обратной совместимости
- Мигрировать `config.py` - оставить как re-export из `core.config`

**Почему именно так:**

- Legacy код (`utils.py`, старый `config.py`) нарушает слоистую архитектуру, затрудняет тестирование и поддержку
- Новая архитектура: Слоистая архитектура (core, infrastructure, application) требует четкого разделения ответственности
- Обратная совместимость: `utils.py` оставлен как wrapper для плавной миграции, `config.py` как re-export из `core.config`

**Где делать изменения:**

- Файлы: `src/infrastructure/output.py` (создать), `src/application/start_service.py`, `src/application/continue_dev_service.py`, `src/application/setup_service.py`, остальные модули
- Функции: Заменить импорты из `utils` на `infrastructure.output`, заменить `read_env_file()` на `FileRepository.read_env_file()`

**Критерии завершения:**

- [x] Модуль `infrastructure/output.py` создан
- [x] Все модули application слоя мигрированы
- [x] `utils.py` преобразован в wrapper
- [x] `config.py` мигрирован как re-export
- [ ] [TODO: Добавить обоснование] Переместить `check_root()` и `get_user()` из `utils.py` в `infrastructure/platform_utils.py`

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 3

---

#### Шаг 2.2: Рефакторинг и уменьшение дублирования

**Статус:** 🟢 Done

**Что нужно сделать:**

- Расширить `ConfigService` методами для получения значений из `.env`
- Вынести форматированный вывод URL и информации в отдельные функции в `infrastructure/output.py`
- Рефакторить `start_service.py` - разбить на меньшие методы
- Устранить дублирование чтения `.env` файла

**Почему именно так:**

- Дублирование кода усложняет поддержку, увеличивает риск ошибок при изменениях
- Централизация: Работа с `.env` через `ConfigService`, форматированный вывод через `infrastructure/output.py`
- Читаемость: Разбиение больших методов на меньшие улучшает читаемость и тестируемость

**Где делать изменения:**

- Файлы: `src/application/services.py` (ConfigService), `src/infrastructure/output.py`, `src/application/start_service.py`
- Функции: `get_env_value()`, `get_virtual_key()`, `is_first_run()`, `print_access_urls()`, `print_api_info()`, `print_status_commands()`

**Критерии завершения:**

- [x] `ConfigService` расширен методами для работы с `.env`
- [x] Функции вывода URL вынесены в `infrastructure/output.py`
- [x] `start_service.py` разбит на меньшие методы
- [x] Дублирование чтения `.env` файла устранено

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 4

---

### Фаза 3: Обработка ошибок и документация

**Контекст:** Улучшение обработки ошибок через кастомные исключения и улучшение документации кода для обеспечения понятности и поддерживаемости.  
**Цель:** Обеспечить информативную обработку ошибок и качественную документацию кода, которая ссылается на документацию (если есть) как источник правды.  
**Статус:** 🟢 COMPLETED

#### Шаг 3.1: Улучшение обработки ошибок

**Статус:** 🟢 Done

**Что нужно сделать:**

- Добавить контекст к исключениям (где это уместно)
- Убедиться, что все исключения наследуются от `AIGatewayError`
- Заменить стандартные исключения Python на кастомные в активном коде
- Улучшить сообщения об ошибках для пользователя
- Улучшить обработку исключений в `cli.py` (более специфичные исключения)

**Почему именно так:**

- Кастомные исключения с контекстом помогают пользователю понять проблему, упрощают отладку
- Цепочка исключений: Использование `from e` сохраняет полный контекст ошибки для отладки
- Логирование: `logger.error()` с `exc_info=True` записывает полный traceback в логи
- Пользовательский опыт: Информативные сообщения с подсказками помогают пользователю решить проблему

**Где делать изменения:**

- Файлы: `src/cli.py`, `src/ports.py`, `src/docker_compose.py`, `src/infrastructure/docker_client.py`, `src/infrastructure/file_repository.py`
- Функции: Заменить `RuntimeError`, `FileNotFoundError` на кастомные исключения, добавить `exc_info=True` в `logger.error()`

**Критерии завершения:**

- [x] Все исключения наследуются от `AIGatewayError`
- [x] Стандартные исключения Python заменены на кастомные в активном коде
- [x] Добавлен контекст к исключениям через `logger.error()` с `exc_info=True`
- [x] Улучшена обработка исключений в `cli.py`

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 5

---

#### Шаг 3.2: Улучшение документации кода

**Статус:** 🟢 Done

**Что нужно сделать:**

- Проверить все публичные функции и классы в `core/`, `infrastructure/`, `application/` на наличие docstrings
- Убедиться, что docstrings следуют Google style (краткое описание + Args/Returns/Raises)
- Добавить ссылки на документацию (если есть) где это уместно
- **НЕ дублировать** детальную информацию из документации в docstrings (если документация есть)

**Почему именно так:**

- Документация (если есть) - единый источник правды, docstrings не должны дублировать её
- Google style: Стандартный формат docstrings, понятный для IDE и инструментов генерации документации
- Ссылки на документацию (если есть): Помогают быстро найти детальную информацию, избегают дублирования

**Принцип:** **Документация (если есть) - источник правды**. Docstrings в коде должны быть краткими и ссылаться на документацию (если она есть), а не дублировать её. Если документации нет, контекст получается через анализ кода.

**Где делать изменения:**

- Файлы: Все модули в `src/core/`, `src/infrastructure/`, `src/application/`
- Формат: `See [путь к документации] for details.` (если документация есть)

**Критерии завершения:**

- [x] Все публичные функции и классы проверены на наличие docstrings
- [x] Docstrings следуют Google style
- [x] Добавлены ссылки на документацию где это уместно
- [x] Детальная информация не дублируется в docstrings

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 7

---

#### Шаг 3.3: Оптимизация импортов

**Статус:** 🟢 Done

**Что нужно сделать:**

- Проверить на циклические зависимости
- Упорядочить импорты (stdlib, third-party, local)
- Использовать `from __future__ import annotations` где нужно
- Проверить неиспользуемые импорты

**Почему именно так:**

- Правильный порядок импортов улучшает читаемость, `from __future__ import annotations` позволяет использовать forward references
- Циклические зависимости: Нарушают архитектуру, затрудняют тестирование
- Неиспользуемые импорты: Увеличивают время загрузки модулей, создают путаницу

**Где делать изменения:**

- Файлы: `src/core/config.py`, `src/infrastructure/security.py` (добавить `from __future__ import annotations`)
- Команды: `ruff check --select I`, `ruff check --select F401`

**Критерии завершения:**

- [x] Циклические зависимости проверены (нет)
- [x] Импорты упорядочены (stdlib, third-party, local)
- [x] `from __future__ import annotations` добавлен где нужно
- [x] Неиспользуемые импорты проверены и удалены

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 8

---

### Фаза 4: Тестирование

**Контекст:** Добавление тестов для гарантии корректности кода, особенно важно для кода, пишущегося AI агентом. Включает unit, integration и E2E тесты.  
**Цель:** Обеспечить 100% покрытие тестами для критичных слоев (core/infrastructure) и полный цикл E2E тестов для готовности репозитория для AI агентов.  
**Статус:** 🟡 IN PROGRESS

#### Шаг 4.1: Unit тесты

**Статус:** 🟢 Done

**Что нужно сделать:**

- Добавить unit тесты для всех модулей в `core/` и `infrastructure/`
- Обеспечить 100% покрытие для критичных модулей
- Проверить соответствие кода документации через тесты

**Почему именно так:**

- Тесты гарантируют корректность кода, особенно важно для кода, пишущегося AI агентом
- 100% покрытие для core/infrastructure: Критичные слои должны быть полностью покрыты тестами
- Документация как источник правды: Тесты проверяют соответствие кода документации, гарантируют консистентность

**Где делать изменения:**

- Файлы: `tests/unit/test_*.py` для каждого модуля
- Модули: `core/exceptions.py`, `core/constants.py`, `core/config.py`, `infrastructure/file_repository.py`, `infrastructure/security.py`, `infrastructure/logger.py`, `infrastructure/output.py`

**Критерии завершения:**

- [x] `core/exceptions.py` - 16 тестов
- [x] `core/constants.py` - 15 тестов
- [x] `core/config.py` - 38 тестов
- [x] `infrastructure/file_repository.py` - 27 тестов (покрытие 92%)
- [x] `infrastructure/security.py` - 21 тест (покрытие 100%)
- [x] `infrastructure/logger.py` - 25 тестов (покрытие 100%)
- [x] `infrastructure/output.py` - 29 тестов (покрытие 85%)

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 6

---

#### Шаг 4.2: Integration тесты

**Статус:** 🟢 Done

**Что нужно сделать:**

- Добавить integration тесты для модулей, которые взаимодействуют с внешними системами
- Использовать моки для внешних зависимостей (Docker API)

**Почему именно так:**

- Integration тесты проверяют взаимодействие между компонентами
- Моки для Docker API позволяют тестировать без реального Docker

**Где делать изменения:**

- Файлы: `tests/integration/test_*.py`
- Модули: `infrastructure/docker_client.py`, `application/services.py`

**Критерии завершения:**

- [x] `infrastructure/docker_client.py` - 39 тестов (покрытие 90%)
- [x] `application/services.py` - 36 тестов (покрытие 88%)

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 6

---

#### Шаг 4.3: E2E тесты

**Статус:** 🔴 Blocked

**Что нужно сделать:**

- Полный цикл setup (non-interactive режим)
- Запуск и остановка контейнеров
- Проверка health checks всех сервисов
- Проверка доступности через HTTP endpoints
- Негативные тесты для логики приложения
- API тесты с моками провайдеров

**Почему именно так:**

- E2E тесты: Обязательны для готовности репозитория для AI агентов - проверяют полный цикл работы
- Принцип E2E тестов: E2E тесты проверяют **только логику приложения**, не системные зависимости
- Системные зависимости (Docker не установлен, недостаток RAM, нет прав) покрыты unit тестами

**Где делать изменения:**

- Файлы: `tests/e2e/test_*.py`, `tests/e2e/conftest.py`
- Команды: `pytest tests/e2e/`

**Критерии завершения:**

- [x] Полный цикл setup (non-interactive режим)
- [x] Запуск и остановка контейнеров
- [x] Проверка health checks всех сервисов
- [x] Проверка доступности через HTTP endpoints
- [x] Негативные тесты для логики приложения
- [x] Базовая структура API тестов с моками провайдеров
- [ ] [TODO: Добавить обоснование] Добавление моделей через API LiteLLM (требует моков провайдеров)
- [ ] [TODO: Добавить обоснование] Тестовые маленькие API запросы к LiteLLM с моками (требует моков провайдеров)

**Связанные вопросы:** Q2.1, Q2.2 в @IMPROVEMENT_QUESTIONS.md (E2E тесты - моки провайдеров)

---

### Фаза 5: Оптимизация архитектуры

**Контекст:** Анализ общих паттернов в сервисах для возможного создания базового класса.  
**Цель:** Определить, оправдано ли создание BaseService для устранения дублирования кода.  
**Статус:** ⚪ PENDING

#### Шаг 5.1: Анализ и создание BaseService

**Статус:** ⚪ Pending

**Что нужно сделать:**

- Проанализировать все сервисы на общие паттерны
- Определить, какие методы можно вынести в базовый класс
- Решить, нужен ли `config_service` в базовом классе или сделать опциональным
- Создать `BaseService` если это оправдано
- Рефакторить существующие сервисы для использования `BaseService`

**Почему именно так:**

- Общие паттерны в сервисах указывают на возможность создания базового класса
- Когда делать: Только если это оправдано - не создавать абстракцию "на будущее"
- YAGNI принцип: Не добавлять функциональность, пока она не нужна

**Контекст:** При рефакторинге `start_service.py` возник вопрос о создании базового класса.

**Анализ общих паттернов:**

- Все сервисы (`StartService`, `SetupService`, `ContinueDevService`) принимают `project_root: Path` в `__init__`
- Все используют `self.project_root = Path(project_root)`
- Все используют `FileRepository(self.project_root)` → `self.file_repo`
- `StartService` и `SetupService` используют `ConfigService(self.project_root)` → `self.config_service`

**Где делать изменения:**

- Файлы: `src/application/base_service.py` (создать, если оправдано), `src/application/start_service.py`, `src/application/setup_service.py`, `src/application/continue_dev_service.py`

**Критерии завершения:**

- [ ] Все сервисы проанализированы на общие паттерны
- [ ] Определено, достаточно ли дублирования для создания BaseService
- [ ] BaseService создан (если оправдано)
- [ ] Существующие сервисы рефакторены для использования BaseService

**Связанные вопросы:** См. @IMPROVEMENT_QUESTIONS.md - BaseService

---

### Фаза 6: Улучшение артефактов плана

**Контекст:** Улучшение читаемости и навигации артефактов плана для работы как с человеком, так и с AI агентом.  
**Цель:** Обеспечить понятность артефактов плана, быструю навигацию и явное выделение блокеров и неотвеченных вопросов.  
**Статус:** 🟢 COMPLETED

#### Шаг 6.1: Улучшение читаемости и навигации артефактов

**Статус:** 🟢 Done

**Что нужно сделать:**

- Добавить в начало `IMPROVEMENT_PLAN.md` раздел "Метаинформация" с статусом, текущей фазой, шагом
- Добавить быструю навигацию для AI агента
- Добавить инструкции по обновлению статуса
- Создать план финальной проверки артефактов

**Почему именно так:**

- Артефакты плана должны быть понятны как человеку, так и AI агенту
- Для людей: Нужно видеть название, описание, статус, текущий шаг сразу при открытии файла (как в Jira тикете)
- Для AI агента: Нужна быстрая навигация к текущему шагу, информация о блокерах и неотвеченных вопросах
- Статусы: Должны отражать реальное состояние (In Progress, Blocked, Needs Review, Completed)

**Где делать изменения:**

- Файлы: `IMPROVEMENT_PLAN.md`, `IMPROVEMENT_PLAN_VALIDATION.md` (создать)

**Критерии завершения:**

- [x] Раздел "Метаинформация" добавлен в начало файла
- [x] Быстрая навигация для AI агента добавлена
- [x] Инструкции по обновлению статуса добавлены
- [x] План финальной проверки артефактов создан

**Связанные вопросы:** См. @IMPROVEMENT_CHANGELOG.md - Этап 10

---

## 🤖 Instructions for AI agent

**Important:** This section contains instructions for working with this artifact. These instructions are self-sufficient and do not require external prompts or templates.

**Artifact System Overview:**

This artifact is part of a system of 4 required artifacts that work together:

1. **PLAN** (`IMPROVEMENT_PLAN.md`) - Execution roadmap with phases and steps. Contains current status, blockers references, and navigation.
2. **CHANGELOG** (`IMPROVEMENT_CHANGELOG.md`) - History of completed changes. Contains chronological entries with what, why, and results.
3. **QUESTIONS** (`IMPROVEMENT_QUESTIONS.md`) - Knowledge base for doubts and solutions. Contains active questions (blockers) and resolved questions.
4. **SESSION_CONTEXT** (`IMPROVEMENT_SESSION_CONTEXT.md`) - Current work state. Contains temporary notes, intermediate decisions, and active context.

**Artifact Relationships:**
- PLAN references blockers in QUESTIONS and recent changes in CHANGELOG
- CHANGELOG entries link to PLAN steps and related questions in QUESTIONS
- QUESTIONS link to PLAN steps and CHANGELOG entries where solutions were applied
- SESSION_CONTEXT tracks current PLAN phase/step and active questions

**When to update artifacts:**
- **PLAN**: When step status changes, when starting/completing steps, when blocked
- **CHANGELOG**: When step completes, when question is resolved, when approach changes
- **QUESTIONS**: When creating new question, when answering question
- **SESSION_CONTEXT**: When starting step, when discovering blocker, when completing step, when making intermediate decisions

**How to read artifacts (created from this template):**
1. Start with section "🚦 Quick Navigation for AI agent" to understand current state (blockers are referenced here)
2. Study current step in section "Фазы имплементации" (Implementation Phases)
3. Follow the instructions in this section for working with the artifact

**How to update artifacts (created from this template):**
1. When step status changes → update metadata at the beginning of file:
   - Update "Status" field
   - Update "Current Phase" and "Current Step" if changed
   - Update "Last Update" date
2. When step completes → update step status (🟢 Done) and metadata:
   - Change step status to 🟢 Done
   - Update phase status if all steps complete
   - Update metadata fields
3. When blocked → update status (🔴 Blocked) and add blocker reference:
   - Change step status to 🔴 Blocked
   - Update phase status to 🔴 BLOCKED if needed
   - Add blocker reference to "🚦 Quick Navigation for AI agent" section
   - Update metadata
4. When starting work → update status to 🟡 In Progress:
   - Change step status from ⚪ Pending to 🟡 In Progress
   - Update metadata
5. After changes → add entry to `IMPROVEMENT_CHANGELOG.md` (see CHANGELOG artifact instructions for procedure)

**Formatting rules:**
- Use exact status icons as defined in status definitions
- Follow structure: Phase → Step → What/Why/Where/Completion criteria
- Use consistent phase/step numbering (Phase X, Step X.Y)
- Links to other artifacts use `@[ARTIFACT_NAME]` notation
- Metadata fields must be updated when status changes

**When to use this file:**
- When starting work on a task from the plan
- When checking current project state
- When deciding on next step
- When updating work status
- When checking blockers and current progress

**Related artifacts:**
- `IMPROVEMENT_QUESTIONS.md` - for checking active questions and blockers
- `IMPROVEMENT_CHANGELOG.md` - for history of completed changes
- `IMPROVEMENT_SESSION_CONTEXT.md` - for current session context
